{% trans_default_domain 'IDGSupervisorBundle' %}

{% if informations %}
    <div>
        {% for key, information in informations %}
            <div> <strong>{{ key }}</strong> : {{ information }} </div>
        {% endfor %}
    </div>
{% else %}
    {{ "information.empty"|trans }}
{% endif %}